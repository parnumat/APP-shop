import 'package:flutter/material.dart';

const zDefaultPadding = 40.0;
const zDefaultRadius = 15.0;

const zTextColor = Color(0xFF707070);
const zTextLightColor = Color(0xFF55555);

final zDefaultShadow = BoxShadow(
    offset: Offset(0, 50),
    blurRadius: 50,
    color: Color(0xFF0700B1).withOpacity(0.15));

final zDefaultCardShadow = BoxShadow(
    offset: Offset(0, 20),
    blurRadius: 50,
    color: Color(0xFF0700B1).withOpacity(0.1));
