import 'package:flutter/material.dart';

class ProfileScreenSpotify extends StatelessWidget {
  const ProfileScreenSpotify({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Profile"),
    );
  }
}
