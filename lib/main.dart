import 'package:flutter/material.dart';
import 'package:shop_app/voice_assistant/main_voice_assistant.dart';

void main() {
  // runApp(MainShopApp());
  // runApp(MainSpotify());
  // runApp(MainOrder66());
  // runApp(MainZeroDawn());
  runApp(VoiceAssistant());
}
