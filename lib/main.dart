import 'package:flutter/material.dart';
import 'package:shop_app/clone_spotify/main_spotify.dart';

void main() {
  // runApp(MainShopApp());
  runApp(MainSpotify());
  // runApp(MainOrder66());
  // runApp(MainZeroDawn());
}
